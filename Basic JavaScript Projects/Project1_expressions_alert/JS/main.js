var A = "\"This is a string\"", B = " and this is a different string";  // Makes the two strings
var A = A.fontcolor("Blue"), B = B.fontcolor("red")                     // Changes the color of the two strings
document.write(A + B);                                                  // Concatenates and prints to page